<%= form_for block, :html => { :id => dom_id(Block.find(block.id))}  do |f| %>
  <%= f.text_area :content, :size => "5x4"  %>
  <%= f.hidden_field :id_case %>
  <%= f.hidden_field :canvas_id %>
  <%= f.submit "Submit", class: "save" %>
<% end %>

